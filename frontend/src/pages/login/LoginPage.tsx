import { PageShell } from '@/shared/layouts/PageShell';

export const LoginPage = () => {
  return (
    <PageShell
      title="Login"
      description="데모 로그인 화면은 Step 2 이후 구현 예정입니다."
    />
  );
};
